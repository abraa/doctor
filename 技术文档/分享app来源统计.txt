 openinstall 第三方组件

 思路:
	1. 在网页通过js获取本机唯一标识
		读写文件: 在本机先通过js保存文件,文件代表本机唯一标识,并通信服务器标识此机
		IP: 通过浏览器webRtc组件获取内外网ip,ip上传服务器绑定此机 (拼拼侠实现)
	2. 在app端获取本机唯一标识
		同1.将标识上传即可在后台判断绑定
	3. 在服务端判断是否来自同一地址
		关联通过就在数据库中把关系对应起来,注意同一IP多次注册.(每次打开未绑定则更新,已绑定则添加,还可以加有效期)